#!/bin/sh

#  cause the script to exit immediately if any command returns a non-zero exit status
set -e

# ensures the pipeline's return status is the value of the last
# (rightmost) command to exit with a non-zero status, or zero if all
# commands succeed.
set -o pipefail

lr1-desktop | mv-here
