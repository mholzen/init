#!/bin/bash

if [ -d "$@" ]; then
  cd "$@"
else
  echo "$@ is not a directory"
  echo "Usage: lr1 [<dir>]"
  exit 1
fi

item=$(lr "$@" | tail -1)

if [ -z "$item" ]; then
  echo "No item found"
  exit 1
fi

realpath "$item"

