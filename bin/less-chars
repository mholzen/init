#!/bin/bash

# cat -v : display non-printing characters so they are visible
# less -X: configure less to not clear the screen when quitting
# less -i: non case sensitive search
cat -v "$@" | /usr/bin/less -Xi
