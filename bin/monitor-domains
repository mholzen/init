cat ~/data/domains | coffee-init -e '
words()
.map (w)->"http://www.#{w}"
.map (d)->get d
.each (p)->
   p.then (r)-> process.stdout.write JSON.stringify {href: r.request.uri.href, statusCode: r.statusCode}
   .catch (r)-> console.log r.request.uri.href, r.statusCode
'
