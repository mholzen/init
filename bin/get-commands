#!/bin/zsh

# Zsh - https://unix.stackexchange.com/questions/120786/list-all-binaries-from-path
function get-commands {
  # whence -pm "*"
  print -rC1 -- ${(ko)commands}
}

function get-commands-full {
  print -rC1 -- ${commands}
}

# TODO: limit to functions in this file
$(basename $0)
