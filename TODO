REQUIREMENTS

- install .dot files on a remote machine

- install some .dot files on a remote machine, depending on whether it makes sense for the target OS
	eg: 'chrome' on a remote server

- can modify files on a remote server and save changes using git

- do as much as possible with python rather than shell scripts
